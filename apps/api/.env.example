# Local dev: MongoDB running via docker-compose (see /docker-compose.yml)
MONGODB_URI=mongodb://localhost:27017/

# Production: Firestore MongoDB-compatibility connection string from Secret Manager
# MONGODB_URI=mongodb+srv://<user>:<pass>@<host>/<db>?authSource=admin

PORT=8080
